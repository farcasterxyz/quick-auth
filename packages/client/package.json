{
  "name": "@farcaster/quick-auth",
  "version": "0.0.1",
  "description": "Minimal client for auth-server API",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "peerDependencies": {
    "typescript": "5.8.3"
  },
  "devDependencies": {
    "vitest": "^1.1.0"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "zod": "^3.25.1"
  }
}
